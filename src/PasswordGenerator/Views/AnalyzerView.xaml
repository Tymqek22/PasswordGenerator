<UserControl x:Class="PasswordGenerator.Views.AnalyzerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordGenerator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400" d:Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Name="PasswordBox" FontSize="14" Padding="5" HorizontalAlignment="Stretch" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
            <ProgressBar Maximum="100" Width="150" Height="20" Value="{Binding StrengthScore}"/>
            <TextBlock Text="{Binding Strength}" Margin="10,0,0,0" FontWeight="Bold"/>
        </StackPanel>



        <StackPanel Grid.Row="7" Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Center" >
            <Button Command="{Binding NavigateToHomeCommand}" Content="Return to Home" Padding="10,5" Margin="0,0,10,0" Height="40" FontSize="14"/>
            <Button Command="{Binding NavigateToGeneratorCommand}" Content="Generator" Padding="10,5" Height="40" FontSize="14"/>
        </StackPanel>
    </Grid>
</UserControl>
