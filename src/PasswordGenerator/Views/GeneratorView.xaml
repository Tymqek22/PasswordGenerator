<UserControl x:Class="PasswordGenerator.Views.GeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordGenerator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400" d:Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="{Binding GeneratedPassword, Mode=OneWay}" Name="PasswordBox" IsReadOnly="True" FontSize="14" Padding="5" HorizontalAlignment="Stretch" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Password Length:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Length, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,10,0,0">
            <CheckBox Content="Include Uppercase Letters" IsChecked="{Binding UseUppercase, Mode=TwoWay}"/>
            <CheckBox Content="Include Lowercase Letters" IsChecked="{Binding UseLowercase, Mode=TwoWay}"/>
            <CheckBox Content="Include Digits" IsChecked="{Binding UseDigits, Mode=TwoWay}"/>
            <CheckBox Content="Include Special Characters" IsChecked="{Binding UseSpecialCharacters, Mode=TwoWay}"/>
        </StackPanel>

        <Button Grid.Row="6" Command="{Binding GeneratePasswordCommand}" Content="Generate Password" Padding="5" Margin="0,10,0,0" Height="40" FontSize="14"/>
    </Grid>
</UserControl>
