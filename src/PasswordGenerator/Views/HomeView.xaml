<UserControl x:Class="PasswordGenerator.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordGenerator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400" d:Background="White">
    <Grid>
        
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Background="#EEE" Height="50" HorizontalAlignment="Center">
            <Button Content="🔐 Generator" Command="{Binding NavigateToGeneratorCommand}" Margin="10" Padding="10,5"/>
            
        </StackPanel>
    </Grid>
</UserControl>
